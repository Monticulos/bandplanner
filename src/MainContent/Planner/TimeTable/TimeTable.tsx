import { Card, Heading } from "@digdir/designsystemet-react";
import React from "react";

export const TimeTable = () => {
  return (
    <Card>
      <Heading level={2}>Time table</Heading>
    </Card>
  );
};
